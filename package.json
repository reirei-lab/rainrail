{
  "name": "rainrail",
  "version": "0.1.0",
  "private": true,
  "description": "Agent-ready development event routing and workflow orchestration suite.",
  "type": "module",
  "packageManager": "pnpm@10.12.1",
  "scripts": {
    "build": "pnpm run build:scripts && pnpm --filter www build",
    "build:scripts": "node scripts/check-scripts.mjs",
    "cf:deploy": "node scripts/validate-cloudflare-secrets.mjs && wrangler deploy",
    "cf:deploy:check": "node scripts/check-cloudflare-deployability.mjs",
    "cf:dev": "wrangler dev --local",
    "cf:smoke": "node scripts/smoke-cloudflare-worker.mjs",
    "docs:check": "node scripts/check-docs-drift.mjs && tsc -p tsconfig.docs.json",
    "pages:build": "pnpm --filter www build",
    "pages:deploy:preview": "pnpm pages:build && wrangler pages deploy apps/www/dist --project-name rainrail-www --branch \"${RAINRAIL_PAGES_BRANCH:-preview}\"",
    "pages:deploy:production": "pnpm pages:build && wrangler pages deploy apps/www/dist --project-name rainrail-www --branch main",
    "pages:smoke": "node scripts/smoke-cloudflare-pages.mjs",
    "test": "vitest run scripts src",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.9",
    "wrangler": "^4.106.0"
  }
}
