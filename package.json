{
  "name": "rainrail",
  "version": "0.1.0",
  "private": true,
  "description": "Agent-ready development event routing and workflow orchestration suite.",
  "type": "module",
  "packageManager": "pnpm@10.12.1",
  "scripts": {
    "build": "node scripts/check-scripts.mjs",
    "typecheck": "tsc --allowJs --checkJs --noEmit --module NodeNext --moduleResolution NodeNext --target ES2022 --lib ESNext --types node scripts/*.mjs",
    "test": "vitest run scripts"
  },
  "devDependencies": {
    "@types/node": "^24.13.2",
    "typescript": "^6.0.3",
    "vitest": "^3.2.3"
  }
}
