name: Cloudflare Docs Pages Deploy

on:
  workflow_run:
    workflows:
      - Pull Request CI
    types:
      - completed
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  actions: read

jobs:
  preview:
    name: Deploy docs preview
    if: >-
      github.event_name == 'workflow_run' &&
      github.event.workflow_run.event == 'pull_request' &&
      github.event.workflow_run.conclusion == 'success' &&
      github.event.workflow_run.pull_requests[0].head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    concurrency:
      group: cloudflare-docs-pages-preview-${{ github.event.workflow_run.pull_requests[0].head.ref }}
      cancel-in-progress: true
    env:
      RAINRAIL_DOCS_BRANCH: ${{ github.event.workflow_run.pull_requests[0].head.ref }}
    steps:
      - name: Check out trusted deploy tooling
        uses: actions/checkout@v7
        with:
          ref: main
          persist-credentials: false

      - name: Set up pnpm
        uses: pnpm/action-setup@v6

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - name: Install trusted dependencies
        run: pnpm install --frozen-lockfile

      - name: Download Cloudflare Docs Pages preview artifact
        id: docs-artifact
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          artifact_id="$(
            gh api "repos/${{ github.repository }}/actions/runs/${{ github.event.workflow_run.id }}/artifacts" \
              --jq '.artifacts[] | select(.name == "rainrail-docs-dist" and .expired == false) | .id' \
              | head -n 1
          )"

          if [ -z "${artifact_id}" ]; then
            echo "::notice::Skipping Cloudflare Docs Pages preview deploy because rainrail-docs-dist was not uploaded for this workflow_run."
            echo "found=false" >> "${GITHUB_OUTPUT}"
            exit 0
          fi

          echo "found=true" >> "${GITHUB_OUTPUT}"
          gh run download "${{ github.event.workflow_run.id }}" --name rainrail-docs-dist --dir apps/docs/dist

      - name: Deploy Cloudflare Docs Pages preview
        if: steps.docs-artifact.outputs.found == 'true'
        env:
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
        run: |
          if [ -z "${CLOUDFLARE_ACCOUNT_ID}" ] || [ -z "${CLOUDFLARE_API_TOKEN}" ]; then
            echo "::notice::Skipping Cloudflare Docs Pages preview deploy because CLOUDFLARE_ACCOUNT_ID or CLOUDFLARE_API_TOKEN is not configured."
            exit 0
          fi

          pnpm exec wrangler pages deploy apps/docs/dist --project-name rainrail-docs --branch "${RAINRAIL_DOCS_BRANCH}"

  production:
    name: Deploy docs production
    if: github.ref_name == 'main' && (github.event_name == 'push' || github.event_name == 'workflow_dispatch')
    runs-on: ubuntu-latest
    concurrency:
      group: cloudflare-docs-pages-production-main
      cancel-in-progress: true
    steps:
      - name: Check out repository
        uses: actions/checkout@v7
        with:
          persist-credentials: false

      - name: Set up pnpm
        uses: pnpm/action-setup@v6

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build Cloudflare Docs Pages production
        run: pnpm docs:build

      - name: Deploy Cloudflare Docs Pages production
        env:
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
        run: |
          if [ -z "${CLOUDFLARE_ACCOUNT_ID}" ] || [ -z "${CLOUDFLARE_API_TOKEN}" ]; then
            echo "::notice::Skipping Cloudflare Docs Pages production deploy because CLOUDFLARE_ACCOUNT_ID or CLOUDFLARE_API_TOKEN is not configured."
            exit 0
          fi

          pnpm exec wrangler pages deploy apps/docs/dist --project-name rainrail-docs --branch main
