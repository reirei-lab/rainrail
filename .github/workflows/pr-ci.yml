name: Pull Request CI

on:
  pull_request:
    types:
      - opened
      - synchronize
      - reopened
      - ready_for_review

concurrency:
  group: pr-ci-${{ github.event.pull_request.number }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  validate:
    name: Typecheck, test, and build
    runs-on: ${{ github.event.pull_request.head.repo.full_name == github.repository && contains(fromJSON('["OWNER", "MEMBER", "COLLABORATOR"]'), github.event.pull_request.author_association) && 'self-hosted' || 'ubuntu-latest' }}
    steps:
      - name: Check out repository
        uses: actions/checkout@v7
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Set up pnpm
        uses: pnpm/action-setup@v6
        with:
          dest: ${{ runner.temp }}/setup-pnpm

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 26
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run typecheck
        run: pnpm typecheck

      - name: Run docs checks
        run: pnpm docs:check
        env:
          DOCS_DRIFT_CHANGED_FROM: origin/${{ github.base_ref }}

      - name: Run tests
        run: pnpm test

      - name: Run Codex App Server smoke test
        if: >-
          github.event.pull_request.head.repo.full_name == github.repository &&
          contains(fromJSON('["OWNER", "MEMBER", "COLLABORATOR"]'), github.event.pull_request.author_association)
        run: pnpm exec vitest run src/codex-app-server/smoke.test.ts
        env:
          RAINRAIL_CODEX_APP_SERVER_SMOKE: '1'

      - name: Run build
        env:
          PUBLIC_RAINRAIL_OPERATIONAL_API_URL: ${{ vars.RAINRAIL_OPERATIONAL_API_URL }}
        run: pnpm build

      - name: Upload Cloudflare Pages preview artifact
        if: >-
          github.event.pull_request.head.repo.full_name == github.repository &&
          !github.event.pull_request.draft
        uses: actions/upload-artifact@v7
        with:
          name: rainrail-pages-dist
          path: apps/www/dist
          if-no-files-found: error

      - name: Upload Cloudflare Docs Pages preview artifact
        if: >-
          github.event.pull_request.head.repo.full_name == github.repository &&
          !github.event.pull_request.draft
        uses: actions/upload-artifact@v7
        with:
          name: rainrail-docs-dist
          path: apps/docs/dist
          if-no-files-found: error

  cloudflare-deployability:
    name: Cloudflare deployability
    needs: validate
    runs-on: ${{ github.event.pull_request.head.repo.full_name == github.repository && contains(fromJSON('["OWNER", "MEMBER", "COLLABORATOR"]'), github.event.pull_request.author_association) && 'self-hosted' || 'ubuntu-latest' }}
    steps:
      - name: Check out repository
        uses: actions/checkout@v7
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Set up pnpm
        uses: pnpm/action-setup@v6
        with:
          dest: ${{ runner.temp }}/setup-pnpm

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 26
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run Cloudflare deployability dry run
        run: pnpm cf:deploy:check

  dashboard-e2e:
    name: Dashboard E2E
    needs: validate
    runs-on: ubuntu-24.04
    steps:
      - name: Check out repository
        uses: actions/checkout@v7
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Set up pnpm
        uses: pnpm/action-setup@v6
        with:
          dest: ${{ runner.temp }}/setup-pnpm

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 26
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Install Playwright browser
        run: pnpm exec playwright install --with-deps chromium

      - name: Run dashboard E2E
        run: pnpm e2e:dashboard

      - name: Collect dashboard VRT results
        id: collect-vrt-results
        if: ${{ always() }}
        run: node scripts/collect-vrt-results.mjs --results-dir test-results/dashboard --report test-results/dashboard/playwright-report.json --output-dir vrt-results

      - name: Generate dashboard VRT comment
        if: ${{ always() && steps.collect-vrt-results.outcome == 'success' }}
        run: node scripts/generate-vrt-comment.mjs --summary vrt-results/summary.json --output vrt-comment.md --max-cases 10

      - name: Upload dashboard E2E artifacts
        if: ${{ always() }}
        uses: actions/upload-artifact@v7
        with:
          name: dashboard-e2e-artifacts
          path: |
            playwright-report/dashboard/
            test-results/dashboard/
            test-results/dashboard/playwright-report.json
            test-results/dashboard/screenshots/
            test-results/dashboard/screenshots/dashboard-demo-screenshot-manifest.json
            vrt-results/
            vrt-comment.md
          if-no-files-found: ignore
          retention-days: 7

  dashboard-vrt-comment:
    name: Dashboard VRT PR comment
    needs: dashboard-e2e
    if: >-
      always() &&
      github.event.pull_request.head.repo.full_name == github.repository &&
      contains(fromJSON('["OWNER", "MEMBER", "COLLABORATOR"]'), github.event.pull_request.author_association)
    runs-on: ubuntu-24.04
    permissions:
      contents: read
      pull-requests: write
    steps:
      - name: Download dashboard VRT artifacts
        uses: actions/download-artifact@v7
        continue-on-error: true
        with:
          name: dashboard-e2e-artifacts
          path: .

      - name: Check dashboard VRT comment artifact
        id: vrt-comment
        run: |
          if [ -f vrt-comment.md ]; then
            echo "available=true" >> "$GITHUB_OUTPUT"
          else
            echo "available=false" >> "$GITHUB_OUTPUT"
            echo "::notice::Skipping dashboard VRT PR comment because vrt-comment.md was not generated."
          fi

      - name: Check CML token availability
        id: cml-token
        env:
          CML_COMMENT_TOKEN_CONFIGURED: ${{ secrets.CML_COMMENT_TOKEN != '' }}
        run: |
          echo "available=${CML_COMMENT_TOKEN_CONFIGURED}" >> "$GITHUB_OUTPUT"
          if [ "${CML_COMMENT_TOKEN_CONFIGURED}" != "true" ]; then
            echo "::notice::Skipping dashboard VRT PR comment because CML_COMMENT_TOKEN is not configured."
          fi

      - name: Set up CML
        if: ${{ steps.vrt-comment.outputs.available == 'true' && steps.cml-token.outputs.available == 'true' }}
        uses: iterative/setup-cml@v2

      - name: Publish dashboard VRT PR comment
        if: ${{ steps.vrt-comment.outputs.available == 'true' && steps.cml-token.outputs.available == 'true' }}
        run: cml comment update --watermark-title="Rainrail dashboard VRT" vrt-comment.md
        env:
          REPO_TOKEN: ${{ secrets.CML_COMMENT_TOKEN }}
